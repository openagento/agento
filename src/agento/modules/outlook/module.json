{
  "name": "outlook",
  "version": "1.0.0",
  "description": "Outlook / Microsoft 365 email channel — poll unread mail, read/reply/mark-read",
  "sequence": [],
  "order": 110,
  "tools": [
    {"type": "mcp", "name": "outlook_list_thread", "description": "List earlier messages + attachment metadata of the triggering Outlook thread (read-only; opt-in via outlook/allow_thread_read)", "toolset": "outlook"},
    {"type": "mcp", "name": "outlook_get_message", "description": "Read a full email by message ID", "toolset": "outlook"},
    {"type": "mcp", "name": "outlook_get_attachment", "description": "Download one attachment from an email to the artifacts dir", "toolset": "outlook"},
    {"type": "mcp", "name": "outlook_reply", "description": "Reply-all on the triggering email thread (whitelist-gated; blocks the whole send if any recipient is not whitelisted)", "toolset": "outlook"},
    {"type": "mcp", "name": "outlook_mark_processed", "description": "Mark an email as read/processed", "toolset": "outlook"},
    {"type": "mcp", "name": "outlook_send_mail", "description": "Send a new email (whitelist-gated)", "toolset": "outlook"}
  ]
}
