[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
    "src/agento/**/*.py",
    "src/agento/**/*.json",
    "src/agento/**/*.js",
    "src/agento/**/*.yml",
    "src/agento/**/*.yaml",
    "src/agento/**/*.md",
    "src/agento/**/*.sql",
    "src/agento/**/*.tcss",
    "src/agento/framework/cli/templates/*",
    "src/agento/toolbox/package.json",
    "src/agento/toolbox/package-lock.json",
    # Local-build distribution context (Dockerfiles + service scripts).
    "src/agento/framework/docker/**/*",
    # Root-level files needed for thin Docker image builds (force-included into wheel below).
    "uv.lock",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agento"]

# Force-include root-level files needed for thin Docker image builds.
# - cron build context needs pyproject.toml + uv.lock for `uv sync` (deps only).
# - toolbox build context needs package.json + package-lock.json for `npm ci`.
# These are copied into the in-package build context at wheel-build time so
# they're available to `agento install` / `materialize_docker_context` at runtime.
[tool.hatch.build.targets.wheel.force-include]
"pyproject.toml" = "agento/framework/docker/cron/pyproject.toml"
"uv.lock" = "agento/framework/docker/cron/uv.lock"
"src/agento/toolbox/package.json" = "agento/framework/docker/toolbox/package.json"
"src/agento/toolbox/package-lock.json" = "agento/framework/docker/toolbox/package-lock.json"

[project]
name = "agento-core"
version = "0.12.2"
description = "AI Agent Framework — automates tasks using AI agents in Docker containers"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "PyMySQL>=1.1",
    "cryptography>=43.0",
    "textual>=1.0,<2.0",
    "regex>=2024.0",
]
authors = [{name = "Marcin Klauza"}]
license = {text = "MIT"}
readme = "README.md"
keywords = ["ai", "agent", "automation", "framework", "docker", "modular"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]

[project.scripts]
agento = "agento.framework.cli:main"

[project.entry-points."agento.modules"]
jira = "agento.modules.jira"
claude = "agento.modules.claude"
codex = "agento.modules.codex"

[project.urls]
Homepage = "https://github.com/agento-cc/agento"
Repository = "https://github.com/agento-cc/agento"
Documentation = "https://github.com/agento-cc/agento/tree/main/docs"
Issues = "https://github.com/agento-cc/agento/issues"
Changelog = "https://github.com/agento-cc/agento/blob/main/CHANGELOG.md"

[tool.pytest.ini_options]
testpaths = ["tests", "app/code"]
markers = [
    "e2e: end-to-end tests that invoke real external binaries / paid APIs / take >5s; skipped by `bin/test --fast`",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["agento"]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-mock>=3.14", "respx>=0.22", "ruff>=0.11", "basedpyright>=1.29"]
test = ["pytest>=8.0", "pytest-mock>=3.14", "respx>=0.22"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.14",
    "respx>=0.22",
    "ruff>=0.11",
    "basedpyright>=1.29",
]
